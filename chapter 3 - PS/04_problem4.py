name = "Utsav is a good  boy and "

print(name.replace("  "," "))
print(name) #Upper one string is new one str, name str is as it is
# Strings are immutable which means  that you cannot
#change them by running functions on them
